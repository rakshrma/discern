# Core DISCERN requirements — API-based evaluation, no GPU needed.
# For local GPU inference (vLLM), also install: requirements-vllm.txt
# For GREEN metric:  requirements-green.txt
# For CRIMSON metric: requirements-crimson.txt

# LLM API clients
openai>=1.0.0
anthropic>=0.25.0

# Data / config
pydantic>=2.0.0
PyYAML>=6.0
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.10.0
tqdm>=4.60.0

# NLP metrics
nltk>=3.8.0
rouge>=1.0.0
bert-score>=0.3.13
evaluate>=0.4.0

# Radiology-specific
radgraph                        # pip install radgraph (RadGraph F1)
RaTEScore                       # pip install RaTEScore

# Install DISCERN package itself (from repo root):
#   pip install -e .
