# CRIMSON metric dependencies.
#
# CRIMSON requires a dedicated conda environment with cutting-edge
# transformers/torch versions that conflict with the main DISCERN env.
#
# Setup:
#   conda env create -f envs/crimson.yml
#   conda activate crimson
#   pip install -e /path/to/CRIMSON  # install CRIMSON from its repo
#
# Then set conda_envs.crimson in config.yaml to:
#   /path/to/conda/envs/crimson/bin/python
#
# Source: https://github.com/rajpurkarlab/CRIMSON

transformers>=5.3.0
torch>=2.10.0
peft>=0.18.1
accelerate>=1.13.0
