name: green
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10          # GREEN requires Python 3.10 for compatibility
  - pip
  - pip:
    - pandas>=1.5.0
    - numpy>=1.23.0
    - tqdm>=4.60.0

    # GREEN: install from GitHub (pinned transformers version for compatibility)
    # pip install git+https://github.com/Stanford-AIMI/GREEN.git

    # After creating this env, set in config.yaml:
    # conda_envs:
    #   green: /path/to/conda/envs/green/bin/python
